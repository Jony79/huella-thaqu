# Avatar por defecto

Opcional: dejá acá una imagen `default.png` (o `.webp`) para quienes todavía no eligieron foto.

Los avatares que sube cada persona **no** van acá: se guardan en el servidor (`uploads/`).
